# Which language the unqualified values/ folder is written in.
#
# generateLocaleConfig builds the app's language list from the values-* folders,
# and it cannot guess what the folder without a qualifier holds. Without this the
# build stops rather than shipping a list that silently omits English.
unqualifiedResLocale=en-US
